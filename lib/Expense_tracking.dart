import 'package:flutter/material.dart';

class ExpenseTracking extends StatelessWidget {
  const ExpenseTracking({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Expense Tracking Screen'),
    );
  }
}

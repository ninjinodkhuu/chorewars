import 'package:flutter/material.dart';

class Gamification extends StatelessWidget {
  const Gamification({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Gamification Screen'),
    );
  }
}
